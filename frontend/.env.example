# Razorpay (https://dashboard.razorpay.com/app/keys) — use Key Id (rzp_test_… / rzp_live_…)
VITE_RAZORPAY_KEY_ID=

# Checkout amount is fixed in code at ₹1 (100 paise) for demos; cards still show ₹999 / ₹2,999.
# For production list-price charges, change checkoutAmountPaise in src/lib/subscriptionPlans.js (and use Orders API + verify).
